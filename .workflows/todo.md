# todo

在 `.todo.md` 中添加新的待办或缺陷条目。
1. 确认要添加的内容和所属章节（如 `# buglist`、`# 待办` 等）；不确定时先询问需求方。
2. 打开 `.todo.md`，定位到对应章节的任务列表。
3. 按现有格式添加条目，使用 Markdown 任务列表写法 `- [ ] 描述`，子项的缩进与周围条目保持一致。
4. 需要直接标记已完成时使用 `- [x]`，否则默认使用未完成格式。
5. 保存后检查排版是否符合现有样式。


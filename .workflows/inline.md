# inline

将指定方法去除封装，改为内联实现。
1. 确认要内联的方法签名、所在文件及需覆盖的调用范围；不明确时先向需求方确认。
2. 打开方法定义，通读方法体，梳理输入输出、副作用和异常路径。
3. 在每个调用点用方法体替换调用：用调用实参替换对应形参，保持控制流与返回逻辑；如需局部变量以承载返回值或中间结果，按原逻辑放置。
4. 确认方法无剩余引用后删除原方法定义，顺带移除已无用的 using 或字段。
5. 进行必要的编译或相关测试验证，确保行为与原实现一致。

